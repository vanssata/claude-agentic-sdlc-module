{
  "case": "a commit message that mentions force push is allowed",
  "expect": "allow",
  "payload": {
    "hook_event_name": "PreToolUse",
    "tool_name": "Bash",
    "cwd": "__ROOT__",
    "tool_input": {
      "command": "git commit -m 'docs: explain why we never git push --force here'"
    }
  }
}
