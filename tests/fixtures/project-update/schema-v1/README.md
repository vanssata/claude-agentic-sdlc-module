A project at schema 1: scaffolded from the v1 templates, `.ai/VERSION` says `1`,
and it has no journal, no handoff and no questions file. The task in flight is
created by `state.py` inside the test rather than written here, so the fixture
never has to be kept in step with the state's shape.
